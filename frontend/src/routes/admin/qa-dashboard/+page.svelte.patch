--- frontend/src/routes/admin/qa-dashboard/+page.svelte
+++ frontend/src/routes/admin/qa-dashboard/+page.svelte
@@ -23,6 +23,8 @@
             }
         } catch (err) {
             error = err instanceof Error ? err.message : String(err);
             console.error("Error fetching QA dashboard data:", err);
+            loading = false;
         } finally {
             loading = false;
         }
